# LearningMachineLearning
## 待办：
- [x] Thelia:手撕一次线性回归！

## 环境

这个项目的主要依赖来自 `Thelia/ProjectSekai/Codes/train.ipynb`。

使用 `uv` 创建并安装虚拟环境：

```bash
uv venv
source .venv/bin/activate
uv pip install -r requirements.txt
```

如果你在 VS Code 里打开 notebook，建议把解释器切换到 `.venv` 对应的 Python。